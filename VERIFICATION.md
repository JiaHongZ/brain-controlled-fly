# 当前版本验证

当前默认转向增益为 2×，`clip(1 - 0.65 * gain * motor / scale, 0.25, 1)`。真实连接矩阵、EEG 分类器和物理参数未变；改变的是明确标注的神经读出到步态控制的工程增益。

## 核心测试

`python -m unittest discover -s tests -v`：9/9 通过，无跳过，36.055 秒。覆盖全部保留连接逐条核对、神经身份与坐标、延迟传播、实际 EEG 输入、因果滤波、MuJoCo 关节与接触、切断突触后的运动因果对照。

切断突触后，左右 EEG 刺激均无法影响基础步态轨迹。此检查证明工程链路的因果依赖，不是生物真实性验证。

## 转向幅度与稳定性

前 24 个真实解码 trial 按原顺序运行，净转角约 22–26°，全部方向匹配，无跌倒。完整两模式结果保存在 `data/validation/gain2-full-replay.json`；运行 `python scripts/audit_gain2.py` 可复现。净转角从第 2 秒累计到第 5 秒，逐步处理航向跨越 ±180° 的情况。匹配解码方向不代表解码等于真实标签。

## 可视化

默认俯视，可切换侧视；大幅方向对比图将解码时朝向统一向上，以虚线显示原方向、实线显示当前方向，扇形角度使用实际物理累计转角，不进行视觉倍增。身体与轨迹使用同一物理帧。显示活动来自后台归一化模型活动，沿边示意粒子已移除。

`turn-presentation.json` 记录左右响应、视角切换、增益操作和移动布局检查。截图：`demo-preview.png`、`right-turn-preview.png`、`mobile-preview.png`。

## 历史证据

`data/validation/baseline-gain1/` 是 1× 增益下的完整运行、逐 trial 对齐、孤立神经刺激及解压运行检查，不能当作 2× 增益下的数值结果。

真实来源是 EEG、EM 连接结构及 micro-CT 身体形态。MANC 为腹神经索而非全脑；动力学与跨系统接口含建模假设，基础行走由已有 CPG 产生。

完整新版结果：Decoded 与 Ground Truth 均完成 144/144 trials，净转向方向各 144/144 匹配驱动类别，最低 upright 分别为 0.98194、0.98195，无跌倒。浏览器控件验证使用按需绘制来避免测试机器软件 WebGL 的绘制开销；应用正常运行以约 30 FPS 调度绘制。

## 中途切断后的剩余转动

新增回归：两份身体先接受相同 EEG 和神经输入 70 次更新，确认 qpos 完全相同；一份切断连接，另一份直接使用零运动群输入。之后连续 50 次更新、跨 trial 比较 qpos，逐步完全相同。切断分支左右运动群均为 0，步幅信号均为 1，但仍出现非零实际转角。这证明剩余旋转来自已存在的物理/控制器状态与基础步态，不是后续 EEG 的控制影响。切断不会撤销过去的运动，也不强制锁定航向。UI 改为独立累计切断后漂移、隐藏 EEG 方向箭头、明确基础步态继续运行。浏览器验证见 `cut-mid-turn-ui.json`。
